#
# Initialise
#

# Load packages
library(stringr)

# Set names
upstream_user_name <- "KITmetricslab" # This is the hub's GitHub name
origin_user_name <- "kassteele" # This is your GitHub name
repos_name <- "RESPINOW-Hub" # This is the name of the hub's repository
local_folder <- "~/Projects" # This is where the repository's clone is
project_folder <- getwd() # This is where the R scripts are (within the RStudio Project)
